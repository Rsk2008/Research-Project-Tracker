# Research Project Tracker - Frontend (React + TypeScript)

Meets the routing and role-based UI requirements. Connects to Spring Boot backend.

## Setup
```bash
npm install
npm run dev
```

Endpoints assumed at `http://localhost:8080`. Update `src/api.ts` if needed.
